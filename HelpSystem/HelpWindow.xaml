<Window
    x:Class="DBF.HelpSystem.HelpWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DBF.HelpSystem"
    Title="{Binding Title}"
    MaxHeight="830"
    MaxWidth="800"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen">

  

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

        <StackPanel Margin="20" Orientation="Vertical">

            <TextBlock
                FontSize="16"
                Text="{Binding Text}"
                TextWrapping="Wrap"
                Visibility="{Binding Text, Converter={StaticResource NullToVisibilityConverter}}" />

            <Grid Visibility="{Binding Image, Converter={StaticResource NullToVisibilityConverter}}">
                <Image
                    RenderOptions.BitmapScalingMode="Fant"
                    Source="{Binding Image}"
                    Stretch="Uniform"
                    StretchDirection="Both" />
            </Grid>

        </StackPanel>
    </ScrollViewer>
</Window>
