<?xml version="1.0" encoding="Windows-1252" ?>
<UserControl
    x:Class="DBF.UserControls.TimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="30"
    d:DesignWidth="150"
    mc:Ignorable="d">

    <ComboBox
        x:Name="PART_Combo"
        MaxHeight="20"
        Padding="4,1"
        HorizontalContentAlignment="Stretch"
        Background="#FF1E1E1E"
        IsEditable="False"
        ItemsSource="{Binding Times, RelativeSource={RelativeSource AncestorType=UserControl}}"
        SelectedItem="{Binding SelectedTime, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        <ComboBox.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding ., StringFormat='{}{0:HH\:mm}'}" />
            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>
</UserControl>