<Window
    x:Class="DBF.Views.ConfirmCloseDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Bekræft"
    MinWidth="380"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    WindowStyle="ToolWindow">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="MessageText"
            Margin="0,0,0,12"
            FontSize="14"
            TextWrapping="Wrap" />

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Margin="0,0,8,0"
                Padding="10,4"
                Click="BtnCancel_Click"
                Content="Annuller" />

            <Button
                Margin="0,0,8,0"
                Padding="10,4"
                Click="BtnSave_Click"
                Content="Luk, men gem status" />

            <Button
                Padding="10,4"
                Click="BtnYes_Click"
                Content="Luk og nulstil" />
        </StackPanel>
    </Grid>
</Window>   